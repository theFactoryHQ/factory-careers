#!/usr/bin/env sh
set -eu

# Git exports repository-local overrides to hooks. Clear them before the
# preflight creates isolated fixture repositories in child processes.
unset GIT_DIR GIT_WORK_TREE

node scripts/validate-current-pr-title.mjs
npm run preflight:pr
